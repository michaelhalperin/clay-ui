# Clay UI — Component Documentation

> **Browse in the browser:** run `npm run docs:dev` from the package root, then open `http://localhost:5177`.

See [index.md](./index.md) for the full component index, or use the sidebar in the VitePress site.
